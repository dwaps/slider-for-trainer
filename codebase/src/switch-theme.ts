// import { DARK_THEMES_KEY, getTheme } from './constants';
// import { getThemes, DARK_THEMES_KEY, importOneTheme } from './init-styles';

// const SwitchButton = document.querySelector('.switch [type=checkbox]') as HTMLInputElement;

// if (localStorage) {
//   SwitchButton.checked = !!localStorage.getItem(DARK_THEMES_KEY);
// }
// const themes = getThemes(SwitchButton.checked);
// importOneTheme(themes);

// SwitchButton?.addEventListener('click', () => {
//   const themes = getThemes(SwitchButton.checked);
//   importOneTheme(themes);
//   if (SwitchButton.checked) {
//     localStorage.setItem(DARK_THEMES_KEY, '1');
//   }
//   else {
//     localStorage.removeItem(DARK_THEMES_KEY);
//   }
//   // location.reload();
//   // themeLink = initTheme(theme);
// })

// export default SwitchButton;
